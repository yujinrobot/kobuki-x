global_costmap:
   global_frame: /map
   robot_base_frame: /base_footprint
   update_frequency: 1.0
   publish_frequency: 0.5
   static_map: true
   transform_tolerance: 0.5

# Ensure that the virtual sensor cannot clear the static map, as of course it cannot "see" the physical obstacles;  NOT NEEDED WITH MULTI-LAYER
   vs_scan:
      clearing: false
