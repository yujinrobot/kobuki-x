global_costmap:
 obstacle_layer:
   observation_sources: ir_scan scan bump
   ir_scan:
     data_type: LaserScan
     topic: ir_scan
     marking: true
     clearing: true
     min_obstacle_height: 0.05
     max_obstacle_height: 0.25 
local_costmap:
 obstacle_layer:
   observation_sources: ir_scan scan bump
   ir_scan:
     data_type: LaserScan
     topic: ir_scan
     marking: true
     clearing: true
     min_obstacle_height: 0.05
     max_obstacle_height: 0.25

